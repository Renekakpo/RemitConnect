package com.example.remitconnect.navigation

/**
 * Interface to describe the navigation destinations for the app
 */
interface RemitNavDestination {
    /**
     * Unique name to define the path for a route(composable)
     */
    val route: String
}